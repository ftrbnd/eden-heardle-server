{
  "name": "eden-heardle",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "heroku-prebuild": "yarn install && turbo run build --filter=eden-heardle-server..."
  },
  "dependencies": {
    "turbo": "^2.5.4"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "typescript": "^5.3.3"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
