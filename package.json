{
  "name": "eden-heardle",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "heroku-prebuild": "turbo build --filter=!./apps/web"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "^5.3.3"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": "22.x",
    "yarn": "^1.22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
